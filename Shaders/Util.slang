
/**
 * Notifies the engine's shader compiler that we want this descriptor to use the _DYNAMIC version of
 * the Vulkan descriptor type.
 */
[__AttributeUsage(_AttributeTargets.Var)]
struct FxDynamic {};
